3/22 기본수학

Vector

축 벡터=어느쪽이 양수이냐?
{1,0}=x의 축벡터

모니터의 안쪽으로들어간다=z축의 축벡터(증가)를 의미한다.
왼손좌표계->(물리배울때 플레밍왼손법칙 비슷하게생김)를 생각하면 그게 x,y,z의 축벡터라고 생각하면된다.

3d란 x,y,z축을 돌리는것이다(데카르트좌표계를 3번돌리는것이다.)

원주율=원의둘레를 원의지름으로나눴더니 3개하고도0.14만큼 남더라->원주율(파이)

지름이 2인 원(둘레가 2파이일때) 3,14=180도로 정의했다(호도법)


벡터의덧셈=이동을원할때
벡터의뺄셈=상대를 바라볼때
벡터의곱셈=벡터의 길이를 늘릴때(방향은 같음)

총알을 날린다고 가정했을때 서로의위치를 뺀값을 그대로더해버리면 날아가는모션으로안보인다.
>>그래서 항상 길이가1인벡터(nomalize)를 이용해서 deltatime에따라 이동함으로서 날아가는거처럼 보이게한다.
>>길이가 왜 1이어야하냐? >>시간이"곱해짐으로서" 일정한 속도로 날아가는거처럼 보여야하기때문에 길이가 곱해도 일정해야한다.




모든 변환은 우리들이 상식적으로 생각하는 대로 될려면 크자이 순서대로 되어야한다.(크기,자전,이동)
크기가 먼저 곱해지거나
위치가 먼저세팅했을때 어떻게 변할지 생각해보고 코드를 치기



///////////크기변환(벡터의 곱셈)
원점을 기준으로 크기가 변한후에 이동해서 보여지는것이다(크기변환을 먼저해야하는이유)
크기가 커진다는건 벡터의Scale을 늘린다는것이다(벡터를 곱한다는것이다)

최초의 버텍스의 위치를 로컬공간이라고한다(크기는 당연히 1)
이동한공간>World Space라고부른다(크기,자전,이동이 모두 적용된 공간)

변환은 항상
로컬 스페이스>월드 스페이스





/////////////이동변환(벡터의 덧셈)
이동변환은 벡터의 덧셈을 이용해서 모든버텍스에 +가되는것이다.





